<h1>¿Olvidaste tu contrase&ntilde;a?</h1>
<hr>
	<%= devise_error_messages! %>
<div class="center">
<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>

		<div class="field camposInline"><%= f.label :email %>
  		<%= f.text_field :email, autofocus: true %></div>

  		<div><%= f.submit "Envíenme las instrucciones para reinicializar la contraseña" %></div>
<% end %>

<%= render "devise/shared/links" %>
	</div>
