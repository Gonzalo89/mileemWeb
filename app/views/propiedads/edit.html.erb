<h1>Editando propiedad</h1>

<%= render 'form' %>

<%= render 'formFotos' %>

<span class="btn"><%= link_to 'Ver publicación', @propiedad %> </span>
<span class="btn"><%= link_to 'Volver', propiedads_path %> </span>
